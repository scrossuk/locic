// Test output.
void testPrint(const char * const str, ...);

class TestClass(int value) {
	static Create(int value) {
		testPrint("TestClass.Create: Value = %d", value);
		return @(value);
	}
	
	int getValue() {
		testPrint("getValue: Value = %d", @value);
		return @value;
	}
	
	void doSomething() {
		testPrint("doSomething: Value = %d", @value);
	}
}

// Entry point.
int testEntryPoint(int argc, char ** argv) {
	auto testInstance = TestClass(10);
	
	auto testInstance2 = TestClass(testInstance.getValue() + 5);
	
	testInstance2.doSomething();
	
	return 0;
}

