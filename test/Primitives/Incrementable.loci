// RUN: %locic %s --emit-llvm -o %t

static assert is_incrementable<byte>;
static assert is_incrementable<short>;
static assert is_incrementable<int>;
static assert is_incrementable<long>;
static assert is_incrementable<long long>;

static assert is_incrementable<unsigned byte>;
static assert is_incrementable<unsigned short>;
static assert is_incrementable<unsigned int>;
static assert is_incrementable<unsigned long>;
static assert is_incrementable<unsigned long long>;

static assert is_incrementable<int8_t>;
static assert is_incrementable<int16_t>;
static assert is_incrementable<int32_t>;
static assert is_incrementable<int64_t>;

static assert is_incrementable<uint8_t>;
static assert is_incrementable<uint16_t>;
static assert is_incrementable<uint32_t>;
static assert is_incrementable<uint64_t>;

static assert is_incrementable<size_t>;
static assert is_incrementable<ssize_t>;
static assert is_incrementable<ptrdiff_t>;

static assert is_incrementable<int*>;
