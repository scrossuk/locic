// RUN: %locic %s --verify > %t
// RUN: FileCheck < %t %s

void function() {
	// CHECK: Element value types 'int8_t' and 'float_t' don't match,
	// CHECK-NEXT: at position
	// CHECK: line 11
	// CHECK-NEXT: column 18
	// CHECK: line 11
	// CHECK-NEXT: column 34
	unused auto v = { 1, 2.0f, 3.0 };
}
