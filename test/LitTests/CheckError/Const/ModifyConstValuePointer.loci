// RUN: %locic %s --verify > %t
// RUN: FileCheck < %t %s

// CHECK: Template arguments do not satisfy require predicate 'satisfies(type: const(int_t), requirement: movable_t)' of method 'assign' at position SourceLocation(filename = {{.*}}/ModifyConstValuePointer.loci, range = (line 7, column 2) to (line 7, column 13)).

void f(const int* value) {
	*value = 1;
}
