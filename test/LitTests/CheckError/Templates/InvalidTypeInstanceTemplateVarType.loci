// RUN: %locic %s --verify > %t
// RUN: FileCheck < %t %s

// CHECK: Template variable 'T' in type '::TestClass' has non-primitive type 'SomeClass', at position SourceLocation(filename = {{.*}}/InvalidTypeInstanceTemplateVarType.loci, range = (line 8, column 11) to (line 8, column 22)).

class SomeClass() { }

template <SomeClass T>
class TestClass() { }
