template <typename T>
require(movable<T> and lessthancomparable<T>)
__primitivefunction T min(T first, T second) noexcept(noexcept_lessthancomparable<T>);

template <typename T>
require(movable<T> and lessthancomparable<T>)
__primitivefunction T max(T first, T second) noexcept(noexcept_lessthancomparable<T>);
