template <typename T>
require(movable<T>)
interface default_constructible_t {
	static T create();
}

template <typename T>
require(movable<T>)
using default_constructible = movable<T> and T : default_constructible_t<T>;
