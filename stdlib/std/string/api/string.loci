import std.string 0.1.0 {
	
	std::string string_literal(const byte* stringConstant);
	
	namespace std {
		
		exception out_of_range();
		
		class string {
			static string create();
			
			static string cstring(const byte* const stringConstant);
			
			string implicitCopy() const;
			
			string add(const string& other) const;
			
			size_t length() const noexcept;
			
			unichar at(size_t index) const;
			
			const byte* const c_str() const noexcept;
			
			compare_result_t compare(const string& other) const noexcept;
		}
		
	}
	
}

