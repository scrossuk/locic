template <typename T>
class unique_ptr(T* ptr) {
	static create(T value) {
		return @(new_raw(move value));
	}
	
	static null() {
		return @(null);
	}
	
	~ {
		delete_raw(@ptr);
	}
	
	lval<T> deref() const {
		return *@ptr;
	}
	
}

