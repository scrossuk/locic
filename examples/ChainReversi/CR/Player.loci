// Chain Reversi Player API.

namespace CR {

	datatype OptionalPosition = Some(Position position) | None();
	
	interface Player {
		OptionalPosition performMove(const Board& board);
		
		void gameOver(bool win);
	}
	
}

